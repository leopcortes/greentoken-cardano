# Chaves privadas (geradas localmente, nunca commitar)
wallet/payment.skey
wallet/payment.vkey
policy/policy.skey
policy/policy.vkey
users/*/*.skey
users/*/*.vkey
users/*/*.pkh

# Transações (geradas pelos scripts)
txs/

# Datums por garrafa (gerados pelo create-bottle.sh e pelo backend)
datums/

# Protocol parameters (gerados localmente)
protocol.json
pp.json
