# Haskell / Cabal
dist-newstyle/
cabal.project.local
.stack-work/
.cache/
*.hi
*.o

# Node.js
node_modules/

# Variáveis de ambiente
.env
!.env.example

# Chaves privadas - NUNCA commitar
*.skey
*.hwsfile
*.priv

# Arquivos gerados pelo cardano-cli (transações, datums por garrafa)
assets/txs/
assets/datums/

# Protocol parameters (gerados localmente via query)
assets/protocol.json
assets/pp.json
protocol.json
protocol-parameters.json
pparams.json
pp.json

# Logs
*.log
logs/
*.error

# Demeter (cache de ambiente cloud - não mais utilizado)
demeter/
workspace/
node-ipc/

# Arquivos de sessão SQL (gerados por IDEs)
*.session.sql

# VSCode
.vscode/
.history/
.claude/
